// Theme exports for easy importing
export { default as theme } from './theme';
export { palette, typographyColors, gradients, shadows } from './palette';

// Export theme provider setup
export { ThemeProvider, CssBaseline } from '@mui/material';
export { createTheme, styled, useTheme } from '@mui/material/styles';