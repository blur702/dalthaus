# Environment Variables Example

# JWT Secret - IMPORTANT: Set this to a secure random string
# This should be the same across server restarts to maintain sessions
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secure-secret-key-here

# Node Environment
NODE_ENV=production

# Server Port
PORT=5001

# Database settings are in /var/www/config/database.json